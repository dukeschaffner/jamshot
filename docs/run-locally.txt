ALL BACKEND SERVICES (toggle in script):
./scripts/run-backend-services.sh
# or: npm run dev:backend


admin ui:
cd admin && npm run dev



# Shared local env lives in env/.env.dev (all services, UI, and CMS read from there).
# Overlay keys: copy env/.env.local.example → env/.env.local
# Named overlays: JAMSHOT_ENV=<name> loads env/.env.dev then env/.env.<name>
# Delete the overlay / unset JAMSHOT_ENV to return to env/.env.dev.
#
# Isolated ephemeral testing: tests/ai-managed/ephemeral-testing/SKILL.md

# Starts DevLog on :5099 first, then API / Stripe / Audio / ProjectWS (etc).
# All service stdout is printed through DevLog with [API]/[Stripe]/… prefixes.
# Plugin (debug builds / MessageStore) and UI (ui/src/lib/logger.js locally) POST to
# http://127.0.0.1:5099/log and appear as [Plugin] / [UI] in the same terminal.
# Cursor MCP "jamshot-dev-logs" tool get_logs reads the same ring buffer
# (optional sources filter: API, UI, Plugin, …).
# First-time MCP setup: cd scripts/dev-log-mcp && npm install  (then reload MCP in Cursor).



RUNNING STRIPE: 
stripe listen --forward-to localhost:5001/api/payments/webhook
if expired: stripe login




RUNNING AUDIO PROCESSING DEV SERVER: 
node ./functions/lambda/audio-processing/dev-start.js




RUNNING VIDEO EXPORT DEV SERVER:
node ./functions/lambda/video-export/dev-start.js

cd /Users/dukeschaffner/Documents/CODING/apps/jamshot/functions/lambda/video-export
./venv/bin/python3 generate_assets.py

cd functions/lambda/video-export
DOTENV_PATH=.env.prod ./venv/bin/python3 generate_assets.py





RUNNING EMAIL NOTIFICATIONS LAMBDA LOCALLY:
node ./functions/lambda/email-notifications/index.js                    # Run based on current date (daily/weekly/monthly schedule)
node ./functions/lambda/email-notifications/index.js daily              # Force daily emails only
node ./functions/lambda/email-notifications/index.js weekly             # Force weekly emails only  
node ./functions/lambda/email-notifications/index.js monthly            # Force monthly emails only
node ./functions/lambda/email-notifications/index.js daily weekly       # Force both daily and weekly emails





RUNNING ISSUES VISUALIZER:
cd issues-visualizer
npm run dev





PROJECTS WS DEV SERVER:
cd functions/lambda/project-ws && npm run dev






CLEAR PROJECT DATA:
cd api/lambda
npm run wipe-projects:confirm      # actually deletes everything