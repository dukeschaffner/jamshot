name: Deploy Competitions Lambda to AWS

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'functions/lambda/competitions/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'test'
        type: choice
        options:
          - test
          - prod
      reason:
        description: 'Reason for deployment'
        required: false
        default: 'Manual deployment'

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-artifact: competitions-lambda-build-${{ github.run_number }}
    defaults:
      run:
        working-directory: ./functions/lambda/competitions/

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js 22
      uses: actions/setup-node@v3
      with:
        node-version: '22'
        cache: 'npm'
        cache-dependency-path: './functions/lambda/competitions/package-lock.json'

    - name: Install dependencies
      run: |
        npm ci

    # - name: Run tests
    #   run: |
    #     npm test

    - name: Create deployment package
      run: |
        # Create deployment zip similar to existing script but CI-friendly
        zip -r competitions-lambda.zip . \
          -x ".env*" \
          -x "test/*" \
          -x "*.md" \
          -x "package-lock.json" \
          -x "scripts/*" \
          -x ".git/*" \
          -x "node_modules/.cache/*"

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: competitions-lambda-build-${{ github.run_number }}
        path: ./functions/lambda/competitions/competitions-lambda.zip

  deploy-to-test:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: competitions-test
      url: ${{ vars.COMPETITIONS_LAMBDA_TEST_URL }}
    if: github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'test')

    steps:
    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ needs.build.outputs.build-artifact }}

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ vars.AWS_REGION_TEST || 'us-east-2' }}

    - name: Deploy to Test Lambda
      run: |
        aws lambda update-function-code \
          --function-name sterio-competition-processor-test \
          --zip-file fileb://competitions-lambda.zip

    - name: Update Lambda configuration
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-competition-processor-test \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_TEST }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_TEST }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            EMAIL=${{ secrets.EMAIL }},
            EMAIL_PASSWORD=${{ secrets.EMAIL_PASSWORD }},
            NODE_ENV=test,
            SMTP_HOST=${{ secrets.SMTP_HOST }},
            SMTP_PORT=${{ secrets.SMTP_PORT }},
            SMTP_SECURE=${{ secrets.SMTP_SECURE }}
          }"

  deploy-to-prod:
    runs-on: ubuntu-latest
    needs: [build, deploy-to-test]
    environment:
      name: competitions-prod
      url: ${{ vars.COMPETITIONS_LAMBDA_PROD_URL }}
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod')

    steps:
    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ needs.build.outputs.build-artifact }}

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ vars.AWS_REGION_PROD || 'us-east-2' }}

    - name: Deploy to Prod Lambda
      run: |
        aws lambda update-function-code \
          --function-name sterio-competition-processor \
          --zip-file fileb://competitions-lambda.zip

    - name: Update Lambda configuration
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-competition-processor \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_PROD }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_PROD }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            EMAIL=${{ secrets.EMAIL }},
            EMAIL_PASSWORD=${{ secrets.EMAIL_PASSWORD }},
            NODE_ENV=production,
            SMTP_HOST=${{ secrets.SMTP_HOST }},
            SMTP_PORT=${{ secrets.SMTP_PORT }},
            SMTP_SECURE=${{ secrets.SMTP_SECURE }}
          }"

    - name: Create deployment tag
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git tag -a "competitions-lambda-v${{ github.run_number }}" -m "Competitions Lambda deployment ${{ github.run_number }}"
        git push origin "competitions-lambda-v${{ github.run_number }}"
