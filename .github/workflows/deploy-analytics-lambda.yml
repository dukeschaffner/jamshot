name: Deploy Analytics Lambda to AWS

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'functions/lambda/analytics/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'test'
        type: choice
        options:
          - test
          - prod
      reason:
        description: 'Reason for deployment'
        required: false
        default: 'Manual deployment'

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-artifact: analytics-lambda-build-${{ github.run_number }}
    defaults:
      run:
        working-directory: ./functions/lambda/analytics/

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js 22
      uses: actions/setup-node@v3
      with:
        node-version: '22'
        cache: 'npm'
        cache-dependency-path: './functions/lambda/analytics/package-lock.json'

    - name: Install dependencies
      run: |
        npm ci

    # - name: Run tests
    #   run: |
    #     npm test

    - name: Create deployment package
      run: |
        # Create deployment zip similar to existing script but CI-friendly
        zip -r analytics-lambda.zip . \
          -x ".env*" \
          -x "test/*" \
          -x "*.md" \
          -x "package-lock.json" \
          -x "scripts/*" \
          -x ".git/*" \
          -x "node_modules/.cache/*"

    # TEMPORARY: Using S3 storage instead of GitHub artifacts
    - name: Configure AWS credentials for S3 upload
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-2

    - name: Upload artifact to S3
      run: |
        aws s3 cp analytics-lambda.zip s3://sterio/lambda-artifacts/analytics-lambda-latest.zip --acl private

    # COMMENTED OUT: Original GitHub artifact upload (uncomment to revert)
    # - name: Upload build artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: analytics-lambda-build-${{ github.run_number }}
    #     path: ./functions/lambda/analytics/analytics-lambda.zip

  deploy-to-test:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: analytics-test
      url: ${{ vars.ANALYTICS_LAMBDA_TEST_URL }}
    if: github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'test')

    steps:
    # TEMPORARY: Using S3 download instead of GitHub artifacts
    - name: Configure AWS credentials for S3 download
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-2

    - name: Download artifact from S3
      run: |
        aws s3 cp s3://sterio/lambda-artifacts/analytics-lambda-latest.zip analytics-lambda.zip

    # COMMENTED OUT: Original GitHub artifact download (uncomment to revert)
    # - name: Download build artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: ${{ needs.build.outputs.build-artifact }}

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ vars.AWS_REGION_TEST || 'us-east-2' }}

    - name: Deploy to Test Lambda - Aggregator
      run: |
        aws lambda update-function-code \
          --function-name sterio-analytics-aggregator-test \
          --zip-file fileb://analytics-lambda.zip

    - name: Wait for code deployment to complete
      run: |
        aws lambda wait function-updated --function-name sterio-analytics-aggregator-test

    - name: Deploy to Test Lambda - Cleanup
      run: |
        aws lambda update-function-code \
          --function-name sterio-analytics-cleanup-test \
          --zip-file fileb://analytics-lambda.zip

    - name: Wait for code deployment to complete
      run: |
        aws lambda wait function-updated --function-name sterio-analytics-cleanup-test

    - name: Update Lambda configuration - Aggregator
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-analytics-aggregator-test \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_TEST }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_TEST }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            NODE_ENV=test
          }"

    - name: Update Lambda configuration - Cleanup
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-analytics-cleanup-test \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_TEST }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_TEST }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            NODE_ENV=test
          }"

  deploy-to-prod:
    runs-on: ubuntu-latest
    needs: [build, deploy-to-test]
    environment:
      name: analytics-prod
      url: ${{ vars.ANALYTICS_LAMBDA_PROD_URL }}
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod')

    steps:
    # TEMPORARY: Using S3 download instead of GitHub artifacts
    - name: Configure AWS credentials for S3 download
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-2

    - name: Download artifact from S3
      run: |
        aws s3 cp s3://sterio/lambda-artifacts/analytics-lambda-latest.zip analytics-lambda.zip

    # COMMENTED OUT: Original GitHub artifact download (uncomment to revert)
    # - name: Download build artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: ${{ needs.build.outputs.build-artifact }}

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ vars.AWS_REGION_PROD || 'us-east-2' }}

    - name: Deploy to Prod Lambda - Aggregator
      run: |
        aws lambda update-function-code \
          --function-name sterio-analytics-aggregator \
          --zip-file fileb://analytics-lambda.zip

    - name: Wait for code deployment to complete
      run: |
        aws lambda wait function-updated --function-name sterio-analytics-aggregator

    - name: Deploy to Prod Lambda - Cleanup
      run: |
        aws lambda update-function-code \
          --function-name sterio-analytics-cleanup \
          --zip-file fileb://analytics-lambda.zip

    - name: Wait for code deployment to complete
      run: |
        aws lambda wait function-updated --function-name sterio-analytics-cleanup

    - name: Update Lambda configuration - Aggregator
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-analytics-aggregator \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_PROD }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_PROD }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            NODE_ENV=production
          }"

    - name: Update Lambda configuration - Cleanup
      run: |
        aws lambda update-function-configuration \
          --function-name sterio-analytics-cleanup \
          --environment Variables="{
            DB_HOST=${{ secrets.DB_HOST_PROD }},
            DB_NAME=${{ secrets.DB_NAME }},
            DB_PASSWORD=${{ secrets.DB_PASSWORD_PROD }},
            DB_PORT=${{ secrets.DB_PORT }},
            DB_USER=${{ secrets.DB_USER }},
            NODE_ENV=production
          }"

    # - name: Create deployment tag
    #   run: |
    #     git config --local user.email "action@github.com"
    #     git config --local user.name "GitHub Action"
    #     git tag -a "analytics-lambda-v${{ github.run_number }}" -m "Analytics Lambda deployment ${{ github.run_number }}"
    #     git push origin "analytics-lambda-v${{ github.run_number }}"
