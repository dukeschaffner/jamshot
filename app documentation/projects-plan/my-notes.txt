


- add better zoom controls (ie mac zoom gesture over the timeline should zoom in and out horizontally)
- make sure new project starts with one track
- project page should be tabbed like track page
    - project management (members, delete, etc)
- what if on clip upload retry, both the original and the new region are uploaded?
- switch to event/operation based instead of state based for project persistence
- make sure temp uploaded files in R2 get deleted
- project storage management (ie if several audio files are uploaded with only 3s regions made from them... storage could get out of control)
    - make sure clips made from the same audio file do not create duplicate assets
- make sure plugin plays in stereo
- need a way to indicate who recorded audio/made what changes
- only sync to plugin when plugin is connected and project is loaded? dont need to show toast on every success


- project web daw is calling /assets and /plugin-payload endpoints too often. we need to optimize this.
- No matching project loaded in plugin. Open the project in the plugin first. - this message displays every time a projec tupdate is made
- should ONLY people who recorded asset and project owner be able to delete assets?
    - make sure to show confirmation if snapshot depends on the asset as well


- why do track reorder ops take so long?
- "this clip overlaps with another clip" - this is showing even when clips are directly back to back





other features:
- project limits/subscription management
- create project from collab tree
    - to make a project from a collab tree, must get permission from root track owner
- inviting users
