track video export:
- waveform viz, showing user profile pics, names, sterio watermark, etc
    - for collabs, divide the screen into a section for each track in the collab so that it can be visualized independently
- vertical orientation
- python lambda function to generate video
- save to R2 bucket (add policy to delete after 3 days of no access)

todo:
- update graphics
- add optimized sterio watermark
- lifecycle rules
- make sure individual track audio is rendered correctly
- add track names
- option to save files locally to dir
- themed videos
    - milestones (50 collabs, 100 likes, 1000 plays, etc)
    - competition announcements
    - collab of the week


add an optional flag -local. when included, the lambda should save all rendered stem audio files and the video file to a local dir (named output) with the track id instead of saving to r2. this is for debugging. it should overwrite the dir every time. 